var should = require("should");

describe("Environment configuration", function () {
    it("should allow the usage of mocha for unit tests", function () {
        "5".should.be.eql("5");
    });
});
